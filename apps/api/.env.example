# Job X-Ray API — copy to .env and fill in values
PORT=8787
# 127.0.0.1 = this Mac only (default). Use 0.0.0.0 in Docker / cloud.
HOST=127.0.0.1
AI_PROVIDER=gemini
GEMINI_API_KEY=
# Fastest Gemini for this app.
GEMINI_MODEL=gemini-3.5-flash-lite
LOG_LEVEL=info